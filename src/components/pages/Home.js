import React from "react";
import MasonryGrid from "../common/Grid/MasonryGrid";
import Layout from "../Layout";

const Home = () => {
  return (
    <Layout>
      <MasonryGrid />
    </Layout>
  );
};

export default Home;
